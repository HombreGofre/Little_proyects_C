#include <stdio.h>      
#include <stdlib.h>  
int main()  
{     
    // use rand() function to generate the number  
    printf (" The random number is: %d", rand() % 100);  
    printf ("\n The random number is: %d", rand() % 100);  
      
    printf (" \n The random number is: %d", rand() % 100);  
    printf ("\n The random number is: %d", rand() % 100);  
    getchar();
   return 0;	
}  
